# Vite 在生产构建（npm run build）时自动加载本文件。
# 默认关闭前端 Mock，便于 ECS / 同源 Nginx 正式部署直连真实后端。
#
# Vercel：根目录 vercel.json 的 build.env 已设置 VITE_USE_MOCK=1，
# 构建时该变量先于 .env.production 生效，预览站仍为 Mock（不会被本文件覆盖）。
VITE_USE_MOCK=0
