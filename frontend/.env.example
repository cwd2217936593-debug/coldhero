# 复制为 .env.local 后选用其一：
#
# 【生产构建】仓库已提交 .env.production（默认 VITE_USE_MOCK=0）。
# ECS 上直接 npm run build 即可；勿删除非你有自定义生产变量。
#
# 【推荐 · 本地联调后端】关闭 Mock，请求走 Vite 代理 /api → http://localhost:4000
# VITE_USE_MOCK=0
#
# 【纯前端 Mock】只开 Mock，不配 VITE_API_BASE_URL：登录与业务均在浏览器本地模拟（无需启动后端）。
# VITE_USE_MOCK=1
#
# 【Mock + 真后端（混合）】须显式写明 API 根地址并启动后端，登录/健康检查/管理员问答才走 localhost:4000。
# VITE_USE_MOCK=1
# VITE_API_BASE_URL=http://localhost:4000
#
# 后端须设置 AI_API_KEY（DeepSeek），管理员账号登录：admin / Coldhero@123（以 seed 为准）
